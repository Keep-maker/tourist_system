-- ============================================================
-- admin 表增加状态字段(用户管理: 启用/禁用)
-- status: 1=启用(默认), 0=禁用
-- ============================================================
ALTER TABLE `admin` ADD COLUMN `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 1=启用, 0=禁用' AFTER `role`;
